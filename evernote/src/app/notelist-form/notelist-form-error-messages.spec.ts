import { NotelistFormErrorMessages } from './notelist-form-error-messages';

describe('NotelistFormErrorMessages', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new NotelistFormErrorMessages()).toBeTruthy();
  });
});
